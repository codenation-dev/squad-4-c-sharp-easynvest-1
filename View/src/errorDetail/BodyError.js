import React, { Component } from 'react';
import { NavLink } from 'react-router-dom';

export default class BodyError extends Component {
    render() {
        return (
            <div>
                {/* aqui tem que ter onde foi o log e quando foi */}
                {/* um título podendo ser o erro em si  */}
                {/* a descrição do erro */}
                {/* botao com o level */}
                {/* eventos */}
                {/* 'coletado por': o token de quem 'fez' o erro na api de erros */}
                {/* um botao de voltar pra home */}
            </div>
        );
    }
}