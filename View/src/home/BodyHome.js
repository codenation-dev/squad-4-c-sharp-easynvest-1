import React, { Component } from 'react';
import { NavLink } from 'react-router-dom';

export default class Home extends Component {
    render() {
        return (
            <div>
                {/* aqui tem que ter o menu com as funcionalidades */}
                {/* dropdown de ambiente */}
                {/* dropdown de ordenação */}
                {/* menu de busca (no wireframe ta um dropdown de busca por: level, descrição, origem) */}

                {/* listagem dos erros com o level - descrição - func. de arquivar - func. de deletar
                divididos por: level | log | eventos (eventos é a frequencia desses logs) */}
            </div>
        );
    }
}